<?php

/**
 * @file
 * Bulk export of default_field_validation_rules objects generated by Bulk export module.
 */

/**
 * Implements hook_default_field_validation_rule().
 */
function ndsu_departments_default_field_validation_rule() {
    $rules = array();

    $rule = new stdClass();
    $rule->disabled = FALSE; /* Edit this to true to make a default rule disabled initially */
    $rule->api_version = 2;
    $rule->rulename = 'Unique Department Value';
    $rule->name = 'unique_department_value';
    $rule->field_name = 'field_ndsu_dept_number';
    $rule->col = 'value';
    $rule->entity_type = 'taxonomy_term';
    $rule->bundle = 'departments';
    $rule->validator = 'field_validation_unique_validator';
    $rule->settings = array(
        'data' => 'bundle',
        'per_user' => 0,
        'bypass' => 0,
        'roles' => array(
            1 => 0,
            2 => 0,
            3 => 0,
        ),
        'errors' => 0,
        'condition' => 0,
        'condition_wrapper' => array(
            'condition_field' => '',
            'condition_operator' => 'equals',
            'condition_value' => '',
        ),
    );
    $rule->error_message = 'The Department Number must be unique.';
    $rules['unique_department_value'] = $rule;

    return $rules;
}
